package com.ltu.m7019eblogapp.utils

object Constants {
    private const val HOST_IP = "localhost" //TODO: Replace localhost with proper host IP if not using emulator
    const val API_BASE_URL = "http://${HOST_IP}:3000"
}